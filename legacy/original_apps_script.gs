/**
 * NRM legacy prototype archive target.
 *
 * Stage 0 intentionally does not extend the old command-parser prototype.
 * Replace this placeholder with the historical Apps Script source when it is
 * available, and keep it reference-only rather than part of the active path.
 */
